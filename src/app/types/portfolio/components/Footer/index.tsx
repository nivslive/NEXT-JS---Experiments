import React, { FC } from "react";
import styles from './style.module.scss';

export const Footer: FC = (props: any): any => {
    return (
        <div className={styles.tableContainer}> 
        Footer!
        </div>
    );
}